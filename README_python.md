# Telegram Schedule Bot (Python/aiogram)

Полнофункциональный бот для Telegram на Python, позволяющий студентам и преподавателям управлять учебным расписанием, получать ежедневные уведомления, проверять текущее занятие и обмениваться обратной связью.

## 📋 Содержание

- [Функциональность](#-функциональность)
- [Технологии](#-технологии)
- [Установка](#-установка)
- [Конфигурация](#-конфигурация)
- [Запуск](#-запуск)
- [Структура проекта](#-структура-проекта)
- [Миграции базы данных](#-миграции-базы-данных)

---

## 🎯 Функциональность

1. **Просмотр расписания**
   - `/day` — расписание на сегодня
   - `/nextday` — расписание на завтра
   - `/date [ГРУППА] DD.MM.YYYY` — расписание на указанную дату
   - `/cur` — текущее занятие

2. **Управление группой**
   - `/add 241-362` — сохранить свою группу
   - `/change_group 241-362` — изменить группу

3. **Уведомления**
   - Ежедневные напоминания о расписании в назначенное время
   - Предупреждения за 5 минут до онлайн-пары
   - `/settings` — выбор времени и типа уведомлений

4. **Сравнение расписаний с учетом локаций** 📍
   - `/compare_groups` — интерактивная команда для поиска общих свободных окон
   - `/compare_groups 221-361 221-365` — прямое сравнение групп на сегодня
   - `/compare_groups 221-361 221-365 60` — найти окна минимум на 60 минут
   - `/compare_groups 221-361 221-365 90 15.10.2025` — окна на 90+ минут на конкретную дату
   - `/compare_groups 221-361 221-365 60 8.10.2025-13.10.2025` — окна на период (максимум 10 дней)
   - **Умный учет локаций:**
     - Группы могут встретиться только если находятся в одном корпусе
     - Если у группы пары на Гражданке, она "привязана" к этой локации весь день
     - Учитываются переезды между корпусами при разных парах
     - Показываются только реальные возможности для встреч

5. **Обратная связь**
   - `/feedback` — отправка отзывов администраторам
   - `/asks` — просмотр и ответ на фидбеки (только для администраторов)

6. **Администрирование** (только глобальные админы)
   - Бан/разбан пользователей (`/ban_user`, `/unban_user`, `/list_bans`)
   - Управление кастомными паттернами (`/add_pattern`, `/remove_pattern`, `/list_patterns`)
   - Управление праздниками и каникулами (`/add_holidays`, `/list_holidays`)
   - Рассылка сообщений `/broadcast`

---

## 🛠 Технологии

- **Python 3.11+**
- **aiogram 3.x** — асинхронный фреймворк для Telegram Bot API
- **SQLAlchemy 2.0** — ORM для работы с базой данных
- **PostgreSQL** — база данных
- **Pydantic** — валидация данных и настроек
- **APScheduler** — планировщик задач (cron)
- **asyncio** — асинхронное программирование
- **Loguru** — современное логирование

---

## 🚀 Установка

1. Клонируйте репозиторий:
   ```bash
   cd python_bot
   ```

2. Создайте виртуальное окружение:
   ```bash
   python3 -m venv venv
   source venv/bin/activate  # Linux/Mac
   # или
   venv\Scripts\activate  # Windows
   ```

3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

4. Установите и настройте PostgreSQL:
   ```bash
   # Ubuntu/Debian
   sudo apt-get install postgresql postgresql-contrib
   
   # macOS
   brew install postgresql
   
   # Создайте базу данных
   createdb schedulebot
   ```

---

## ⚙ Конфигурация

1. Создайте файл `.env` на основе `.env.example`:
   ```bash
   cp .env.example .env
   ```

2. Заполните переменные окружения в `.env`:
   ```env
   BOT_TOKEN=your_telegram_bot_token
   DB_HOST=localhost
   DB_PORT=5432
   DB_USER=postgres
   DB_PASSWORD=your_password
   DB_NAME=schedulebot
   ADMIN_USER_IDS=343695019
   ```

3. Настройте список администраторов в `ADMIN_USER_IDS` (через запятую)

---

## ▶️ Запуск

1. Примените миграции базы данных:
   ```bash
   alembic upgrade head
   ```

2. Запустите бота:
   ```bash
   python main.py
   ```

Или используйте Docker (если настроен):
```bash
docker-compose up -d
```

---

## 📂 Структура проекта

```
python_bot/
├── main.py                      # Точка входа приложения
├── config.py                    # Конфигурация и настройки
├── requirements.txt             # Зависимости Python
├── .env.example                 # Пример переменных окружения
├── README.md                    # Документация
│
├── bot/                         # Основной код бота
│   ├── __init__.py
│   ├── handlers/                # Обработчики команд и событий
│   │   ├── __init__.py
│   │   ├── commands.py          # Обработчики команд
│   │   ├── schedule.py          # Команды расписания
│   │   ├── admin.py             # Административные команды
│   │   ├── feedback.py          # Обратная связь
│   │   ├── settings.py          # Настройки пользователя
│   │   └── callbacks.py         # Callback-кнопки
│   │
│   ├── services/                # Бизнес-логика
│   │   ├── __init__.py
│   │   ├── schedule.py          # Сервис расписания
│   │   ├── notification.py      # Сервис уведомлений
│   │   ├── feedback.py          # Сервис обратной связи
│   │   └── tutorial.py          # Интерактивное обучение
│   │
│   ├── middleware/              # Middleware для обработки
│   │   ├── __init__.py
│   │   ├── auth.py              # Проверка прав
│   │   ├── rate_limit.py        # Rate limiting
│   │   └── logging.py           # Логирование
│   │
│   ├── utils/                   # Утилиты
│   │   ├── __init__.py
│   │   ├── message_queue.py     # Очередь сообщений
│   │   ├── text_utils.py        # Форматирование текста
│   │   └── keyboards.py         # Клавиатуры
│   │
│   └── filters/                 # Фильтры aiogram
│       ├── __init__.py
│       └── admin_filter.py      # Фильтр для админов
│
├── database/                    # База данных
│   ├── __init__.py
│   ├── models.py                # SQLAlchemy модели
│   ├── session.py               # Управление сессиями
│   └── repository.py            # Репозитории для работы с БД
│
├── alembic/                     # Миграции базы данных
│   ├── versions/
│   └── env.py
│
└── alembic.ini                  # Конфигурация Alembic
```

---

## 🗄 Миграции базы данных

Для создания новой миграции:
```bash
alembic revision --autogenerate -m "описание изменений"
```

Для применения миграций:
```bash
alembic upgrade head
```

Для отката миграции:
```bash
alembic downgrade -1
```

---

## 📝 Основные команды бота

### Для пользователей:
- `/start` — начать работу с ботом
- `/help` — список команд
- `/day` — расписание на сегодня
- `/nextday` — расписание на завтра
- `/cur` — текущее занятие
- `/add 000-000` — установить группу
- `/settings` — настройки уведомлений
- `/feedback` — отправить отзыв

### Для администраторов:
- `/asks` — просмотр фидбеков
- `/ban_user` — забанить пользователя
- `/unban_user` — разбанить пользователя
- `/list_bans` — список банов
- `/add_holidays` — добавить каникулы
- `/broadcast` — рассылка сообщений
- `/stat` — статистика бота

---

## 🔧 Разработка

### Архитектура

Бот построен по сервисной архитектуре с использованием aiogram 3.x:
- **Handlers** — обработчики команд и событий
- **Services** — бизнес-логика (расписание, уведомления)
- **Middleware** — промежуточная обработка (auth, rate limit)
- **Repository Pattern** — работа с базой данных
- **Message Queue** — очередь для обхода лимитов Telegram

### Очередь сообщений

Реализована локальная очередь сообщений на основе `asyncio.Queue`:
- Автоматическое управление rate limits Telegram
- Приоритизация сообщений
- Retry механизм при ошибках
- Поддержка batch-отправки

### Особенности реализации

1. **Асинхронность** — все операции выполняются асинхронно
2. **Type Safety** — использование Pydantic для валидации
3. **Middleware** — централизованная обработка запросов
4. **FSM** — управление состояниями через aiogram FSM
5. **Logging** — структурированное логирование через Loguru

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

---

## 🤝 Вклад в проект

1. Создайте ветку: `git checkout -b feature/название`
2. Внесите изменения и проверьте код
3. Отправьте Pull Request

---

## 📞 Поддержка

При возникновении проблем создайте Issue в репозитории с подробным описанием.
